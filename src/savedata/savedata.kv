#:kivy 1.0.9

RootWidget:
    AtomWidget:
        color: .800, .800, .800
        init_text: 'p'
        pos: 200, 100
    EllipseWidget:
        color: .800, .800, .800
        pos: 400, 100
        AtomWidget:
            init_text: 'p'
            pos: (self.parent.x + self.parent.size[0]/2, self.parent.y + self.parent.size[1]/1.2)
        SquareWidget:
            pos: (self.parent.x + self.parent.size[0]/2, self.parent.y + self.parent.size[1]/2)
    EllipseWidget:
        color: .800, .800, .800
        pos: 70, 100
        AtomWidget:
            init_text: 'p'
            pos: (self.parent.x + self.parent.size[0]/2, self.parent.y + self.parent.size[1]/1.2)
